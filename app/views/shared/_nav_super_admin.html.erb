<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="<%= asset_path('nav_super_admin.css') %>" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script>
        $(document).ready(function() {
            $('#submit_search').click(function(i) {
                i.preventDefault();

                let search = $('#search_input').val();
                if (search) {
                    window.location.href = '/search/' + search;
                }
            });
        });
    </script>
</head>
<body>
    <nav>
        <div id="nav_logo">
            <a href="<%= home_path %>"><img src="<%= asset_path('mylogo.png') %>" alt="logo"></a>
            <a href="<%= add_movies_path %>" class="add_movies"><h2>Add Movie</h2></a>
        </div>

        <div id="search_bar">
            <form id="search_form" class="search_bar">
                <input type="text" name="search" placeholder="Search" class="search" id="search_input">
            </form>
            <a href="#" id="submit_search"><img src="<%= asset_path('search.png') %>" alt="search logo" class="search_logo"/></a>
        </div>
        <div id="nav_user">
            <a href="<%= admins_path %>"><h2>Add Admin</h2></a>
            
            <div class="dropdown">
                <button><h2>User</h2></button>
                <div class="content">
                    <h6>Hi! <%= current_user.first_name %></h6>
                    <a href="<%= profile_path %>">Profile</a>
                    <a href="<%= watchlist_path %>">Watchlist</a>
                    <a href="<%= browse_path('all') %>">Browse</a>
                    <a href="<%= rated_path %>">Rating</a>
                    <a href="<%= signout_path %>">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
